# Настройка Яндекс.Диска для автобэкапа Remnawave

## Добавленный функционал

В скрипт `backup-restore.sh` добавлена поддержка загрузки бэкапов на Яндекс.Диск.

### Новые возможности:

1. **Загрузка бэкапов на Яндекс.Диск** - альтернативный способ хранения бэкапов помимо Telegram и Google Drive
2. **Настройка папки назначения** - возможность указать конкретную папку на Яндекс.Диске
3. **Уведомления в Telegram** - после успешной загрузки на Яндекс.Диск отправляется уведомление в Telegram

## Как настроить

### 1. Получение OAuth токена

1. Перейдите на страницу: https://yandex.ru/dev/disk/poligon
2. Нажмите кнопку **"Получить OAuth-токен"**
3. Авторизуйтесь в своем аккаунте Яндекса
4. Разрешите приложению доступ к Яндекс.Диску
5. Скопируйте полученный токен

### 2. Настройка в скрипте

Запустите скрипт и выберите:
```
5. Настройка способа отправки
3. Установить способ отправки: Яндекс.Диск
```

При первой настройке вам будет предложено:
- Ввести OAuth токен
- Указать путь к папке на Яндекс.Диске (например: `backups/remnawave`)
- Или оставить пустым для загрузки в корневую папку

### 3. Изменение настроек

Для изменения настроек Яндекс.Диска:
```
6. Настройки конфигурации
3. Настройки Яндекс.Диск
```

Доступны опции:
- Изменить OAuth токен
- Изменить путь к папке

## Технические детали

### Добавленные переменные в config.env:
- `YD_TOKEN` - OAuth токен для доступа к Яндекс.Диску
- `YD_FOLDER_PATH` - путь к папке на Яндекс.Диске (опционально)

### API Яндекс.Диска

Используется REST API Яндекс.Диска:
- Получение ссылки для загрузки: `GET /v1/disk/resources/upload`
- Загрузка файла: `PUT` по полученной ссылке

Документация API: https://yandex.ru/dev/disk/api/

### Автоматические уведомления

После успешной загрузки бэкапа на Яндекс.Диск, в Telegram отправляется уведомление с информацией:
- ✅ Статус создания бэкапа
- 🌊 Версия Remnawave
- 📁 Состав бэкапа (БД + директория)
- 📏 Размер архива
- 📅 Дата и время создания

## Безопасность

⚠️ **Важно:**
- OAuth токен хранится в файле `/opt/rw-backup-restore/config.env`
- Права доступа к файлу автоматически устанавливаются на `600` (только владелец)
- Не передавайте токен третьим лицам
- При компрометации токена - отзовите его и создайте новый

## Проверка работоспособности

После настройки можно проверить работу:
```
1. Создать бэкап
```

Скрипт автоматически:
1. Создаст архив с бэкапом
2. Загрузит его на Яндекс.Диск в указанную папку
3. Отправит уведомление в Telegram
4. Удалит старые бэкапы согласно политике хранения

## Устранение неполадок

### Ошибка при загрузке
- Проверьте правильность OAuth токена
- Убедитесь, что на Яндекс.Диске достаточно места
- Проверьте, что указанная папка существует или оставьте путь пустым

### Токен не работает
- Получите новый OAuth токен
- Убедитесь, что разрешили доступ к Яндекс.Диску при создании токена

## Дополнительная информация

- Файлы автоматически перезаписываются если файл с таким именем уже существует
- Размер файла ограничен лимитами Яндекс.Диска (обычно несколько ГБ)
- Поддерживается автоматическая отправка по расписанию через cron
